# 2026-07-25 — 004 hai loại hình (một pháp nhân, hai sổ) + trim DB local còn 3 pilot

## What Was Done

- **ADR #19 (grill-with-docs + domain-modeling):** phát hiện 004 (MST `0901051747`) KHÔNG phải 2 chế
  độ hải quan — là **1 DNCX có 2 SỔ QUYẾT TOÁN** khác loại hình (sổ vật tư tự sở hữu "EPE" + sổ gia
  công "GC"). Tờ khai là 1 list DNCX (E11/E15/E42) **dùng chung, nhân đôi** lên 2 company row (id9
  EPE, id10 GC — byte-identical 229 tờ/547 dòng). Verified: **C1.2 = 99 finding, 91 GIẢ** (mã thuộc
  sổ kia bị báo "thiếu M15": GC 70=66+4, EPE 29=25+4). Cả bản gộp Tầng-1 (219) lẫn 2-row (187) đều
  nhiễm, ngược chiều nhau.
- **Mô hình chốt:** 1 pháp nhân = 1 sổ tờ khai HQ dùng chung + N sổ quyết toán, mỗi sổ 1 **loại hình**
  (enum `CompanyType`). Check cross-layer (C1.1/C1.2/C1.4) đối chiếu tờ khai với UNION các sổ; check
  nội-sổ (C4.1/C4.3/C6.1/C3.3) `GROUP BY book`.
- **Code (test-first, 680 pass, ruff sạch):** cột nullable `book` trên nvl/sp/norms/findings (migration
  `c9d0e1f2a3b4`, down `b8c9d0e1f2a3`). Sửa 6 check: **C1.1/C1.4 gộp theo `(mã, đơn vị)`** (không cộng
  MTR+ROLL); **C4.1/C4.3/C6.1 `GROUP BY book`**; **C3.3** đơn vị per-`(book,mã)`. 8 check row-wise
  (C1.3/1.6/1.7 + C2.1–2.4 + C5.1) gắn `book=r.book` + `book` vào evidence filter (Option X — truy nguồn
  per-sổ). Guard `_guard_single_book` (ingest.py) chặn re-ingest pháp nhân nhiều sổ (+2 test). **book=NULL
  = pháp nhân 1 sổ → hành vi cũ không đổi.**
- **Collapse 004 trên DB LOCAL:** id9+id10 → 1 row `PILOT_004`/`DEMO_004`; dedup tờ khai (547); books
  tag (nvl EPE104/GC37, sp 43/2, norm 664/216); re-run → **C1.2 99→4, tổng 187→74, risk 30**. E2E proof:
  `.ai/features/2026-07-25-004-two-loai-hinh/` (2 screenshot + ui_smoke.py + brief.md).
- **Trim DB local còn 3 pilot:** GIỮ PILOT_002 (48), PILOT_006 (14876), PILOT_004 (74). XOÁ DN_001–005,
  ZZ_DEMO, DN_GATE + toàn bộ FK rows (13 bảng company_id). Re-run cả 3 trên code mới; **002/006 identical**
  (single-book → new code no-op → xác nhận không regression).
- **Sửa schema drift DB local:** local ở alembic `f5a6b7c8d9e0`, THIẾU `company_periods.data_version`
  (model bắt buộc) + `book` → `run_checks` CHẾT ("no such column") + server :8200 (`--reload` đã nạp
  model mới có `book`) **500 trên trang settlement/findings**. Sửa bằng **ALTER trực tiếp** (data_version
  + book). Server OK lại (healthz 200, /companies 303). Memory `local-db-schema-drift`.

## Decisions Made

- **Từ "loại hình" (per-sổ, khớp enum `CompanyType`) KHÔNG "chế độ"** — owner chốt.
- **Data shape:** gộp 2 row → 1 pháp nhân + book-tag (KHÔNG giữ 2-row-union-lúc-check). ADR #19.
- **Gán book theo `company_id` nguồn (9→EPE, 10→GC), KHÔNG parse `source_file`** — tên GC thực tế
  `(GC.)`/`(GC)`/`-GC`, match literal `"(GC)"` trượt 2/3.
- **C1.1/C1.4 khoá `(mã, đơn vị)`** — 6 mã EPE có 2 dòng M15 = cùng mã hai đơn vị (MTR+ROLL). Khớp
  đơn vị **phía tờ khai** = pre-existing single-book, HOÃN.
- **UI breakdown EPE/GC: HOÃN sang session sau, GRILL/design trước khi build.**
- Advisor (Fable) review: xác nhận seam S1/S2 + Option X; bắt bug source_file, ingest guard, dual-unit,
  collapse-scope ngoài Tier-1, evidence book filter.

## What Didn't Work

- Advisor đề "SUM theo mã" cho C1.1/C1.4 — QUÁ THÔ (dual-unit MTR/ROLL, cộng across đơn vị vô nghĩa)
  → sửa thành `(mã, đơn vị)`.
- `alembic upgrade head` trên DB local **FAIL**: migration `b7d2e1f4a3c6` `create_table('check_runs')`
  lỗi vì bảng đã tồn tại (drift — bảng có nhưng alembic_version cũ). → dùng ALTER trực tiếp.
- Playwright form-login lần đầu ra trang login: **KHÔNG phải do Secure cookie** (cookie
  `httponly+samesite=lax`, KHÔNG Secure) — do smoke `seed()` **thiếu `db.commit()`** (`create_user` chỉ
  `flush`, Session context manager không auto-commit). Thêm commit → login OK trên http localhost. Đã
  sửa memory `playwright-login-via-cookie`.

## Open Items

- **CHƯA commit** toàn bộ: 16 file M (6 check + 2 model + ingest + conftest + 5 test + ADR#19 + GLOSSARY)
  + feature dir + migration `c9d0e1f2a3b4` + `tests/test_ingest_guard.py`. `uv.lock` untracked.
- **PROD chưa đụng:** prod ở `b8c9d0e1f2a3` (= down_revision của migration mới, prod KHÔNG drift như
  local) → `alembic upgrade head` trên prod áp `c9d0e1f2a3b4` SẠCH. Prod hiện chỉ 002/006, chưa có 004;
  đưa 004 lên prod = upgrade migration + collapse 004 trên prod.
- **UI 2 sổ:** grill + design session sau (badge `book` trên finding có book + đếm "sổ EPE: N · sổ GC: M"
  ở header 004). Hiện `book` chỉ ở tầng dữ liệu, KHÔNG route/template nào render.
- Backup local: `audit_hq.sqlite.bak-pre-004-collapse-20260725-020819` + `bak-pre-trim-3pilots-20260725-023941`
  (mỗi cái +`-wal`/`-shm`). Restore = cp đè `audit_hq.sqlite*`.
