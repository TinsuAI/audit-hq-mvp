# 2026-08-08 — Cài bốn vé #111 · #115 · #112 · #114 (đêm)

Phiên chạy không người trực. Yêu cầu: cài hết loạt vé sinh ra từ grill #110, song song
được thì giao agent, khó thì hỏi advisor.

## Kết quả

| Vé | Trạng thái | Commit | Seed xáo | Ghi chú |
|---|---|---|---|---|
| #111 S1 | **Đã gộp** `main` | `57b8b6f` → merge `9c4269b` | 4271458340 | Đóng luôn #109 |
| #115 ĐVT | **Đã gộp** `main` | `50a374c` → merge `abbfccf` | 2980988246 | Agent nền |
| #112 S2 | **Đã gộp** `main` | `8b65837` → merge `0017ff1` | 3430776063 | Có ảnh E2E |
| #114 cột (9) | **Đã gộp** `main` | `f12bd25` → merge `9dfb697` | 1340095647 | Agent nền |
| #113 S3 | **Đã gộp** `main` | `46f9325` → merge `350d016` | 4164449907 | Khép loạt #110 |

Bộ test: **1.724** test trên `main` đã gộp (1.662 lúc mới xong #111 — #112/#114/#115
thêm phần còn lại). Lượt xác nhận cuối trên `main` đã gộp: `PYTEST_EXIT=0`, seed xáo
**331433647**. Seed ở bảng trên là seed TRƯỚC khi gộp của từng vé. Mọi lượt chạy dùng
`DATABASE_URL` trỏ file scratch. `ruff check app tests` sạch ở mọi commit.
**DB dev `audit_hq.sqlite` KHÔNG bị đụng tới** (mtime vẫn 07/08) — mọi phép đo chạy
trên bản sao lấy bằng `.backup`, không phải `cp`.

## Việc đã làm

**#111 — đảo chiều suy bằng chứng.** Module khai mới `app/adapters/declared_fields.py`
(nhãn tiếng Việt · bắt buộc theo biểu · khoá dòng · nhóm cột được). `FIELD_LABEL_VI`
chuyển về đây, 5 chỗ import trỏ lại. m15/m15a/m16 suy bằng chứng TỪ map cột như bcct.
Phủ: m15 11/11 · m15a 10/10 · m16 8/8 · bcct giữ 100%.

**Hai thứ tìm thêm trong lúc cài, không có ở vé:**

1. **`_EVIDENCE_ORDER` (`data_files.py`) là chỗ giấu THỨ HAI.** Nó là danh sách giữ tay
   lọc khối "căn cứ đọc" xuống đúng `("material_code", "norm_qty")` cho m16. Phủ đủ bằng
   chứng thôi thì `product_code` VẪN không hiện — tiêu chí nghiệm thu của #109 vẫn trượt.
   Đã suy nó từ tập trường khai, nên thêm trường là hiện luôn.
2. **`review_state` chặn theo `is_consumed`**, nên phủ thêm cột mô tả (tên, ĐVT, STT)
   KHÔNG làm phồng cổng review. Chỉ trường có check đọc mới bắn — đúng ý #109.

**#115 — tách "không tra được" khỏi "lệch đơn vị".** Thêm outcome `UNRESOLVED`;
C3.3 trả WARNING kèm tên đúng chuỗi không tra được thay vì CRITICAL. Đo trên bản sao
DB: 148 → 144 phát hiện, critical 145 → 94. **Cổng 94 giữ nguyên: 0 phát hiện có mọi
đơn vị đã tra được bị đổi mức.**

Agent bắt được một hồi quy giữa chừng nhờ chính cổng đó: thêm bí danh `GÓI`→`PKG` làm
`'Kiện/Hộp/Bao/Gói'` NGỪNG tra được (trước đó chỉ tra ra `BOX` vì `HỘP` là phần duy nhất
nhận ra được; có phần thứ hai thì thành nhập nhằng), hạ 2 phát hiện thật từ critical
xuống warning. Sửa bằng fallback theo family trong `compare()`.

**Không tra được hết 25 chuỗi — 17/25.** Bảy chuỗi cố ý để nguyên: `UNL` (277 dòng),
`UNK`, `I/át`, `I/at`, `1000 viên`, cộng `Real Brasil` và `Panh` (vé đã loại trừ).
`UNL`/`UNK` đúng nghĩa là ô "không rõ", và `I/át` không đối chiếu được với nguồn nào.
Gán bí danh cho chúng là khẳng định một tương đương không có căn cứ — mà nửa sau của
chính vé này sinh ra để chúng ra WARNING thay vì CRITICAL, và nay đúng như vậy.

**#112 — màn gán cột theo trường.** Một dòng mỗi trường khai, ba trạng thái, cột mới
`saved_column_maps.absent_fields`, cảnh báo tách hai mức theo hậu quả (thiếu khoá dòng
→ từ chối; thiếu bắt buộc-không-khoá-dòng → nhận + đánh dấu). Adapter chụp cột của file
lúc parse (chỉ số · tiêu đề · 3 mẫu giá trị) để bộ chọn hiện «cột 5 · «Tồn đầu» ·
1.234 / 0 / 88». Bất biến `column_map ∩ absent_fields = ∅` khẳng định ở MỘT chỗ.

**#114 — bộ mã cột (9) theo kỳ.** `m16_note.py` khoá bộ mã theo ngày KẾT THÚC kỳ,
`TT121_EFFECTIVE = 2026-02-01`. Giá trị ngoài bộ mã đếm vào `ParseIssues` và nổi lên
trang file, không sửa giá trị. C4.1 delta = 0 trên 190 cặp (note, kỳ) / 270.385 dòng.

**Giới hạn có chủ ý của #114, mới chỉ ghi trong docstring:** gọi `parse_m16` mà KHÔNG
truyền `period_to` thì hàm suy kỳ là 31/12 của năm. DN có niên độ lệch dương lịch mà kỳ
kết thúc trong khoảng 01/01–31/01/2026 sẽ bị xếp nhầm vào TT 39 thay vì TT 121.
`ingest.py` chưa truyền được cửa sổ kỳ thật vì `resolve_period_bounds` cần phiên DB mở
SAU khi parse. **Không DN nào trong kho dính** (mọi DN đang là niên độ dương lịch), và
tham số đã có sẵn để người gọi truyền vào khi cần.

## Quyết định trong lúc cài

- **`m16.note` và `norm_qty` không được chung một cột.** Bố cục 004 có hai cột định mức;
  `_detect_actual_norm_col` dời `norm_qty` sang cột ĐM thực tế, và chỉ số đó chính là cột
  `note` → adapter đọc MỘT cột vào HAI trường (đo: `norms.note` == `norm_qty` ở 880/880
  dòng PILOT_004 2025). Nay trường mất chỗ về *chưa gán*, không bịa vị trí khác. C4.1
  không đổi vì giá trị đụng độ là SỐ, chưa bao giờ thoả `is_domestic_origin`.
- **Ô trống ở biểu mẫu mang hai nghĩa khác nhau** nên phải phân biệt bằng cờ
  `_field_major`: biểu theo-trường luôn gửi giá trị cho mọi trường (bộ chọn có mục
  "— chưa gán —") nên ô trống ở đó là LỰA CHỌN bỏ gán; biểu cũ chỉ gửi ô của trường cần
  soát nên ô trống vẫn là "giữ đề xuất". Đoán nhầm thì hoặc đánh rơi cột, hoặc không bao
  giờ bỏ gán được.
- **Trường khai chưa gán KHÔNG vào cổng review.** Không đọc cột nào thì không có bằng
  chứng để soát; việc phải làm với nó nằm ở cảnh báo thiếu trường. Nhét vào cổng làm
  phồng số "cột cần xác nhận" bằng những dòng không đọc gì.
- **Màn giữ CẢ hai trục**: trạng thái gán và cổng review là hai câu hỏi khác nhau.

## Sai lầm trong phiên

- **Lệnh nền đầu tiên không chạy gì mà vẫn thoát 0.** `rm -f base.sqlite*` với glob không
  khớp → zsh **huỷ cả lệnh** (`no matches found`), nên `pytest` phía sau không bao giờ
  chạy, và mã thoát vẫn 0. Đây đúng là kiểu hỏng làm cả đêm hôm trước không ra gì.
  **Không dùng glob trần trong lệnh nền** — viết đủ tên file kèm `-wal`/`-shm`.
- **Sửa `review` làm mất kết luận của parser.** Nhánh mới đọc `evidence` trước
  `meta["review"]`, nên trường có `review` lưu sẵn mà không có khoá `evidence` bị tính
  lại thành `needs_review`. Bắt được vì `test_preview_grid_page` đỏ. Thứ tự đúng:
  `meta["review"]` thắng, y như trước.
- **Bỏ badge review khỏi template** khi thêm cột trạng thái gán — `test_review_screen`
  đỏ. Vé thêm trạng thái, không bảo bỏ cổng review.
- **Ba lượt chạy bộ test bị bỏ phí** vì sửa code trong lúc chúng đang chạy. Lần sau: chốt
  code rồi mới chạy lượt xác nhận.

## Rà soát hai trục (`/code-review e0863b3..HEAD`)

Chạy sau khi gộp. **Đã sửa trong phiên** (kèm test cắn được):

- **Trục Spec — lỗi thật ở #112 của tôi:** trường NHÓM CỘT dựng CẢ `<select>` lẫn ô chữ
  cùng `name="col_X"`. Starlette dựng dict biểu mẫu theo kiểu ô sau đè ô trước, nên
  select thành ô câm và cột cán bộ vừa chọn biến mất KHÔNG BÁO — đúng loại file (#95)
  mà màn này sinh ra để cứu. Nay trường nhóm chỉ còn ô chữ.
- **Trục Spec — `changed_fields` bỏ sót ca bỏ gán:** trường rời khỏi map vì "chưa gán"
  không vào diện chạy lại, nên phát hiện cũ vẫn nói theo cột vừa gỡ. Nay mọi trường rời
  khỏi map đều vào diện.
- **Trục Spec — `_note_code_hint()` nêu bộ mã của CẢ HAI thế hệ** cho mọi kỳ, ngay trong
  câu "ngoài bộ mã của kỳ này". Trái thẳng yêu cầu lõi của #114. Nay `ParseIssues` mang
  luôn bộ mã đã dùng để phán, câu cảnh báo trích đúng bộ mã của kỳ đó.
- **Trục Standards — vị ngữ "không tra được" định nghĩa HAI nơi:** `compare()` dùng
  `resolve_families()` rỗng, còn `c3_classify` dùng `resolve_canonical() is None`. Chuỗi
  ghép nhập nhằng canonical (`Kiện/Hộp/Bao/Gói`) tra được HỌ nhưng không chốt được
  canonical → bị nêu tên trong câu "không có trong bảng đơn vị chuẩn", mà nó CÓ trong
  bảng. Nay một vị ngữ `uom.is_unresolvable()` dùng chung.
- **Trục Standards — `<th>Cổng review</th>`** là chuỗi tiếng Anh lọt vào văn bản cán bộ
  đọc (AGENTS.md buộc UI thuần Việt) → "Cổng soát cột". Kèm gỡ nhánh switch thứ hai trên
  `state` trong template (`state_badge`), và nối `tests/test_uom.py` vào danh sách nguồn
  duy nhất `_LEFT_UNRESOLVED_ON_PURPOSE` thay vì chép lại.

**CHƯA sửa — việc sáng mai:**

- **"Chưa gán" KHÔNG bền vững.** Bỏ gán một trường thì lượt parse sau áp lại hằng cột mặc
  định của adapter, nên trường quay về "đã gán". Chỉ "xác nhận vắng" mới bền. Hoặc bỏ mục
  "— chưa gán —" khỏi bộ chọn khi trường có vị trí mặc định, hoặc lưu thêm trạng thái —
  quyết định thiết kế, không sửa vội lúc 5h sáng.
- **Test cha-con của #111 chưa thật sự dời cột** — nó khẳng định forward-fill nhưng không
  đổi vị trí cột mã SP, nên yếu hơn tiêu chí nghiệm thu ghi trong vé.
- **Chuẩn hoá lại các mốc số của #115**: vé ghi 46/140, chú thích code ghi 53/148, phiên
  này đo 148→144. Ba con số ở ba thời điểm dữ liệu khác nhau, cần chốt một mốc.
- Vài mục nhỏ hơn: `_report_parse_issues` gọi `file_read_basis` không truyền
  `absent_fields` (vô hại vì `save_column_map` chặn giao), và `_basis_column` dựng lại
  bảng tra khai mỗi trường.

## #113 — cài sau, cùng phiên

Cổng tiền-dispatch mở từ mức NGUỒN xuống mức TRƯỜNG. Adapter đóng dấu tập trường vắng
vào `parse_detail` lúc nạp (không đọc thẳng `saved_column_maps`: map khoá theo
`(DN, slot, vân tay)` nên dùng chung giữa các KỲ, còn trường vắng là chuyện của một kỳ).
Ánh xạ trường→check đi qua đúng `checks_reading()` mà màn gán dùng, nên cảnh báo và hành
vi lúc chạy không lệch nhau được.

**Hai chỗ vé nói sai, đã đo lại bằng đồ thị gọi:**

1. Vé liệt **10** check đọc `declaration_lines`. Đo ra **tám** — C1.7 và C5.1 chỉ đọc
   `NvlBalance`. Khai bừa hai mã đó là dựng cổng `not_evaluable` GIẢ cho check vẫn kết
   luận được.
2. Chỉ khai trường mà THIẾU NÓ THÌ KẾT LUẬN KHÔNG ĐỨNG VỮNG. `value_total` cố ý để
   ngoài: C1.1 đọc nó qua `valuation.py` để gắn số tiền vào phát hiện, nhưng thiếu nó
   C1.1 vẫn phát hiện đúng chênh lệch.

**Cách biểu diễn cho `denominators.py` / `company_type.py` / `scope.py`** (câu hỏi vé
giao lại): quy phần đọc của chúng về CHÍNH các mã check GỌI chúng, không bịa mã giả.
`declaration_date` vào mọi mục bcct vì `scope.py` dựng cửa sổ kỳ bằng nó — thiếu nó thì
không dòng tờ khai nào vào phạm vi.

Test hygiene registry nay còn khẳng định mọi tên trường khai đều CÓ trong tập trường khai
của biểu đó — gõ sai một tên thì `checks_reading()` không khớp, cổng câm đúng cột đó, và
trước đây không gì đỏ.

**Màn gán cột dựng lại theo chiều DỌC** (yêu cầu trong phiên): mỗi trường một khối, bộ
chọn chiếm cả chiều rộng, và MẪU GIÁ TRỊ THẬT của cột đang gán thay cho chỉ số cột trần.
"cột 7" không nói được máy đọc đúng ô hay lệch một ô; `1.5 · 1.51 · 1.52` thì nói được.

## Còn mở

- **DB dev chưa migrate.** Hai revision mới (`a4b5c6d7e8f9` #115, `b5c6d7e8f9a0` #112)
  mới chỉ chạy trên DB scratch. Chạy `alembic upgrade head` trên `audit_hq.sqlite` là
  việc cần người quyết — sao lưu kèm `-wal`/`-shm` trước.
- **`/code-review` cả loạt** `e0863b3..HEAD` chưa chạy.
- 25 worktree agent cũ còn tồn ở `.claude/worktrees/`.
