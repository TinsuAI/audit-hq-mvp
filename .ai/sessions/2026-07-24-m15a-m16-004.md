# Session 2026-07-24 — M15a mở rộng + M16 ĐM thực tế cho 004 + badge truy nguồn

Tiếp sau C1 (đã deploy). Hoàn thành phần "CHƯA thực hiện — Mẫu 15a" của ADR #15 → **ADR #17**.
**CHƯA commit, CHƯA deploy.** Screenshot: `.ai/features/2026-07-24-m15a-m16-004/`.

## What Was Done

**0. Đổi tên hiển thị 4 DN pilot (theo yêu cầu, CHỈ tên):** `company.name` → `DEMO_002`,
`DEMO_006`, `DEMO_004_EPE`, `DEMO_004_GC` (local DB). `code`/`tax_id`/`address` GIỮ nguyên
(user chốt "chỉ tên"). Backup DB trước: `audit_hq.sqlite.bak-relabel-20260724`. name non-empty
→ sống sót re-ingest. **MST + file Excel tải xuống vẫn là THẬT** (ngoài phạm vi "chỉ tên").

**1. Tầng parse (ADR #17):**
- `app/adapters/extended_layout.py`: `resolve_m15a` + `select_extended_m15a` + `parse_formula_terms`
  (giữ nhóm chữ) + `_identity_counts`. Cổng đẳng thức + `export_qty` theo NHÃN (duy nhất, phải
  là số hạng trừ) + khai triển `(8ab)`. Thêm `formula/checked/matched` vào `ColMap` (cho badge M15).
- `app/adapters/m15a.py`: đường mở rộng fallback khi `select_sheet` trượt + `provenance`.
- `app/adapters/m16.py`: `_detect_actual_norm_col` (chọn c8 "thực tế" khi có cặp kỹ thuật/thực tế).
- `app/adapters/m15.py`: `provenance` cho đường mở rộng (004 NVL cũng lệch chuẩn).
- `app/adapters/_common.py`: `ParseProvenance` (layout + detail).
- `app/pipeline/discover.py`: `content_slots` dò thêm `_extended_m15a_ok` cho slot m15a.

**2. Lưu bằng chứng + badge:**
- Migration `f5a6b7c8d9e0`: `data_files.parse_layout` + `parse_detail`. Model + `parse_detail_obj`.
- `ingest.py` `IngestStats.provenance` (slot→ParseProvenance); `data_files.record_parse_result`
  ghi vào DataFile.
- `company_documents.html`: badge/file. `company_data.html` + route: note trên bảng.
  `style.css`: `.parse-note`.

**3. Test:** `test_m15a_extended.py` (6), `test_m16_actual_norm.py` (3), `test_parse_provenance_ui.py`
(2). Full suite **583 pass**, ruff clean.

## Verify (dữ liệu thật, local DB)
- **004 EPE:** M15a 43 dòng (đẳng thức 43/43, export "…xuất kho năm nay/Export this year" cột 10),
  M16 664 dòng (c8 thực tế). Phát hiện 111→98, **C4.3 fire 8**.
- **004 GC:** M15a 2 dòng (2/2), M16 216. **C1.4 fire 2** (sau khi sửa kỳ — xem dưới).
- **006 + 6 DN whitelist:** đường CHUẨN, không đụng.
- **Harness** (seed_uom + ingest + run_checks toàn whitelist vào DB rỗng): **419 → 419 y hệt**
  từng DN (stash code cũ đo lại cũng 419). Trung tính.
- Badge render OK (curl grep + 2 screenshot Playwright inject cookie).

## Decisions Made (chi tiết ở ADR #17)
- `export_qty` theo NHÃN, KHÔNG theo số biểu (M15a không ổn định giữa DN). 3 cổng: đẳng thức +
  nhãn duy nhất + phải là số hạng trừ. Không đủ → không nạp.
- Discovery chỉ dò mở rộng cho m15a (m15 nhận theo tên file).
- M16 override chỉ khi có CẶP kỹ thuật/thực tế → whitelist bất biến.
- Badge có LƯU (migration) thay vì chỉ hiện lúc nạp — user chọn Option 1.
- **Kỳ GC:** dùng manual-override ADR #16 (không sửa `default_bounds`) — an toàn cho whitelist.

## What Didn't Work / Ghi chú số liệu
- **"1.331" trong ADR #16/STATUS KHÔNG khớp fresh-ingest whitelist (419).** Đo lại code cũ cũng
  419. 1.331 chắc đo bằng nguồn khác (DB local có inject / gồm pilot). Cần chốt MỘT harness chuẩn.
  Xem memory [[harness-baseline-methodology]]. Với đợt này điều QUAN TRỌNG là **delta = 0**.
- **Header GC ghi sai kỳ** (M15/M15a = FY2024, thực tế FY2025) → `ingest` lấy header M15 → cửa sổ
  rỗng → bcct=0. Đã đặt `company_periods` manual GC 2025 = 2025-04-01..2026-03-31.
- **Playwright form-login trượt** (cookie Secure/HttpOnly over http + rate-limit tích luỹ). Chụp
  bằng cách curl login lấy cookie `ahq_session` rồi `ctx.add_cookies`. Server throwaway :8322
  (setsid, kill theo PID — KHÔNG đụng :8200).

## Open Items
- **CHƯA commit/deploy.** Nhiều file thay đổi + migration `f5a6b7c8d9e0` (mới áp LOCAL; prod vẫn
  `e4f5a6b7c8d9`). Deploy → code lên prod nhưng **prod chưa có file 004** → cần upload data mới thấy.
- **C4.3 nhánh hệ số nhân sản lượng M16** vẫn CHẶN bởi đề án (B2/B4) — cột sản lượng M16 giờ đã
  xác định (664/664) nhưng đổi định nghĩa phải update `../audit-hq/` trước.
- **Banner "dữ liệu mẫu" + MST thật** vẫn tồn tại (chỉ đổi `name`, không đổi MST/file) — chặn đưa
  pilot lên demo công khai.
- Local DB đã đổi: 004 re-ingest (M15a/M16 mới), GC manual period, 4 DN đổi name. Backup:
  `audit_hq.sqlite.bak-relabel-20260724`.
