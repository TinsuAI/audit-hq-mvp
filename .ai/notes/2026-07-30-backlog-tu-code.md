# Backlog rút ra từ code (không phải từ doc) — 2026-07-30

Phương pháp: quét trực tiếp `app/`, `scripts/`, `tests/` bằng grep có chủ đích (không đọc lại các doc đã có), đối chiếu route đăng ký với test, đối chiếu `CheckSpec` trong registry với test file tương ứng. Không sửa file nào, không thao tác git.

## Số lượng theo nhóm

| Nhóm | Số lượng |
|---|---|
| `TODO`/`FIXME`/`XXX`/`HACK` trong code Python (`app/`, `scripts/`, `tests/`) | **0** (chuỗi `XXX` duy nhất khớp là trong blob base64 của `app/static/showcase.html`, không phải comment) |
| Comment "chưa/tạm/sau này" mang nghĩa **công việc dở dang thật sự** (sau khi loại các câu mô tả trạng thái hợp lệ, ví dụ docstring kiểu "None nếu chưa cấu hình") | **5** |
| Route đăng ký trong `app/routes/*.py` | **80** |
| Route không có bất kỳ test nào gọi tới (xác minh bằng 2 vòng: regex full-path rồi grep từ khoá rời để loại false-negative do f-string) | **22** |
| `CheckSpec` đăng ký trong `app/checks/registry.py` | **17** (C1.1–C1.4, C1.6, C1.7, C2.1–C2.4, C3.1–C3.3, C4.1, C4.3, C5.1, C6.1) |
| Check không có test hoặc test không assert trên finding | **0** — mỗi check đều có test riêng theo mẫu `test_cX_Y_fires_when_...` / `test_cX_Y_no_fire_when_...`, assert trực tiếp trên finding, không chỉ chạy hàm |
| `except` nuốt lỗi đã rà (bare `except`, `except Exception`, `except (...): pass`) | 13 vị trí rà; **4** thực sự im lặng không log, còn lại có `log.exception(...)` hoặc là fallback best-effort có chủ đích |
| Bản sao logic đã biết (`max(company_year_scores)`) lặp lại ở nơi khác | **4** vị trí khác ngoài cặp đã biết (`companies.py:181-197` vs `ai/tools.py:548,567`) |

Dead code: không rà hết được trong ngân sách thời gian; chỉ ghi nhận 1 trường hợp rõ ràng (status `not_evaluable` khai trong model nhưng 0 nơi dùng).

## Bảng backlog (xếp theo hệ quả người dùng thấy, không theo độ dễ sửa)

| Mức độ | Vị trí | Vấn đề | Hệ quả người dùng thấy | Việc phải làm |
|---|---|---|---|---|
| Cao | `app/pipeline/export.py:85`, `app/ai/sql_tool.py:101`, `app/ai/tools.py:394`, `app/ai/tools.py:733` | Đọc thẳng `company.risk_score` (cache) thay vì `max(CompanyYearScore.score)` tươi. `app/routes/companies.py:181-197` đã sửa lỗi "cache có thể drift khỏi điểm theo năm" (đổi rule/chạy lẻ không recompute) cho trang danh sách, nhưng export Excel, SQL tool của AI, và 2 hàm khác trong `app/ai/tools.py` chưa áp dụng | File Excel xuất cho khách, câu trả lời của trợ lý AI trong chat, và context AI đọc khi giải thích điểm rủi ro có thể hiển thị điểm khác với trang danh sách/chi tiết DN cho cùng một DN | Trích hàm tính `max(CompanyYearScore)` dùng chung (đang lặp logic ở `companies.py:195-199`), thay 4 điểm đọc trên bằng hàm đó |
| Cao | `app/routes/admin_ai.py:133,165,191,221,249,260,308` | 7 route cấu hình AI (connection, models, limits, fallback, flags, test-connection, xem hội thoại theo DN) — 0 test gọi tới | Nếu handler lưu sai (không persist, thiếu validate, đổi flag không có tác dụng), lỗi chỉ lộ ra khi admin bấm nút trên UI thật; đã từng xảy ra sự cố tương tự (key OpenRouter 401 mà `test_connection()` báo OK giả vì gọi nhầm endpoint public) | Viết test HTTP cho 7 route này, ít nhất kiểm tra: lưu xong đọc lại đúng giá trị, endpoint test-connection phân biệt được key đúng/sai |
| Cao | `app/routes/companies.py:862,931,952` | `/companies/{code}/documents/upload`, `.../file/{file_id}/delete`, `.../file/{file_id}/download` — 0 test | Đây là 3 thao tác lõi của luồng nạp dữ liệu (tải file DN lên, xoá, tải xuống); một regression ở đây chặn đường nạp dữ liệu cho toàn bộ DN mà không có gì báo trước khi chạy CI | Thêm test route-level cho cả 3, gồm case lỗi (file sai định dạng, file không tồn tại khi xoá/tải) |
| Cao | `app/checks/scoring.py:55` | `_active_tiers()` bắt `except Exception` bao quanh `get_tiers()` rồi âm thầm trả về `TIERS` (ngưỡng hard-code cũ), không log | Nếu cấu hình ngưỡng rủi ro trong DB bị hỏng (giá trị sai kiểu, thiếu dòng), toàn bộ tier hiển thị (màu, nhãn mức rủi ro) trên trang danh sách/chi tiết DN âm thầm quay về ngưỡng mặc định cũ, không có dấu vết nào trong log để biết đang dùng cấu hình nào | Thu hẹp `except Exception` xuống loại lỗi thực sự có thể xảy ra (DB context thiếu), log rõ khi rơi vào fallback |
| Cao | `app/app_settings.py:97`, `app/app_settings.py:178` | `except (ValueError, TypeError): pass` khi parse JSON của `risk_tier_uppers` và `combos_enabled` đã lưu trong `AppSetting`, không log | Admin đổi ngưỡng rủi ro hoặc bật/tắt combo check trong `/admin`, hệ thống báo lưu thành công, nhưng nếu giá trị lưu bị hỏng thì đọc lại âm thầm rơi về default — admin không có cách nào biết cấu hình của mình không được áp dụng | Log khi parse thất bại, cân nhắc chặn lưu giá trị không hợp lệ ngay tại đường ghi thay vì chỉ bắt lỗi ở đường đọc |
| Vừa | `app/routes/companies.py:349,617,1530,1589` | `/companies/new`, `/companies/{code}/diagnose-ai`, `/companies/{code}/overview-batch`, `/companies/{code}/overview.json` — 0 test | Form tạo DN mới, chẩn đoán AI khi nạp lỗi, và cơ chế sinh tổng quan hàng loạt (ADR #21 mục 9) không có lưới an toàn tự động | Thêm test route-level, ưu tiên `overview-batch` vì đây là job chạy nền, lỗi khó phát hiện thủ công |
| Vừa | `app/routes/admin_users.py:52,137` | `/admin/users/create`, `/admin/users/{user_id}/delete` — 0 test (route `/scope` và `/change-password` đã có test) | Tạo/xoá tài khoản cán bộ là thao tác nhạy cảm về bảo mật, không có test tự động xác minh quyền hạn hay ràng buộc (ví dụ xoá admin cuối cùng) | Thêm test cho cả hai, gồm case quyền (non-admin gọi phải bị chặn) |
| Vừa | `app/routes/admin.py:39,89,118,133` | `/admin/units`, `/admin/units/aliases` (POST), `/admin/units/aliases/{alias_id}/delete`, `/admin/units/canonical` — 0 test (`tests/test_uom.py` chỉ test logic UOM thuần, không gọi route) | UOM alias sai ảnh hưởng trực tiếp kết quả các check phụ thuộc đơn vị tính (C1.1, C1.4, C1.7, C3.3 — theo ghi chú "Mẫu 15a cột nhập và cơ chế quyết định cột" đã từng gặp lỗi đọc nhầm cột do UOM) nhưng route quản trị UOM không có test | Thêm test route-level cho CRUD alias/canonical |
| Vừa | `app/checks/c4_norm.py:144-147` | Comment tự ghi: bố cục Mẫu 16 mở rộng có cột sản lượng theo khối chưa được xử lý — `Norm` chưa có cột sản lượng nên nhánh tính `Σ(định_mức_khối × sản_lượng_khối)` "chưa dựng được" | Nếu một DN dùng bố cục Mẫu 16 mở rộng này, C4.3 sẽ cộng dồn định mức qua từng dòng lặp (nhân trùng số lần lặp khối) mà không có cảnh báo nào — kết quả sai âm thầm, không phải lỗi crash | Xác nhận với đề án liệu bố cục này có xuất hiện trong 6 DN pilot; nếu có, thêm cột sản lượng vào `Norm` và rẽ nhánh tại điểm đã đánh dấu |
| Vừa | `app/routes/admin_checks.py:330` | `/admin/checks/{check_id}/draft` (đưa check về nháp) — 0 test | Thao tác này đổi trạng thái publish của một check đang chạy cho khách; lỗi ở đây có thể khiến check biến mất khỏi kết quả pilot mà không ai kiểm tra | Thêm test route-level |
| Vừa | `app/checks/registry.py:376` (comment tại `CHECK_COLUMNS`) | Cột `name`/`unit` (dùng cho C3.3) và cột BCCT/HS (C3.1, C3.2, C6.1) "chưa mô hình hoá ở WS1" trong bảng khai nguồn bằng chứng cột | Theo cơ chế evidence-based confidence đã chốt (WS1, ADR #18), các check này không có cùng cơ chế tính độ tin cậy nguồn cột như nhóm check đã khai — không rõ với người dùng vì không có cảnh báo, chỉ thiếu trong bảng khai | Bổ sung khai báo cột cho 3 check này vào `CHECK_COLUMNS` hoặc xác nhận WS1 cố ý bỏ qua và ghi rõ lý do |
| Vừa | `app/models/check_run.py:35` | Status `not_evaluable` khai trong model, comment "DÀNH SẴN (Tầng C chờ họp) — chưa build"; grep toàn repo cho `not_evaluable` chỉ ra đúng 1 chỗ — chính comment này | Tầng C (nhóm check chờ họp quyết định, theo đề án) không có đường thể hiện trạng thái "không đánh giá được" cho người dùng cuối; mọi check hiện chỉ có `ok`/`error` | Khi Tầng C được chốt trong đề án, cần build đường set + hiển thị status này; hiện tại chỉ cần biết đây là placeholder chưa dùng |
| Thấp | `app/checks/c1_quantity.py:4` | Docstring ghi rõ C1.5 (tái xuất M15 vs B13) là "W.I.P theo §4.1 — chưa implement" | Không có finding nào cho luồng tái xuất — nếu người dùng kỳ vọng theo catalog 49 check thì thiếu, nhưng vì MVP chỉ chọn 16 check nên đây là thiếu có chủ đích, không phải bug | Đối chiếu §4.1 đề án xem C1.5 có nằm trong 16 check MVP không; nếu không, xoá comment gây hiểu nhầm là "còn nợ" |
| Thấp | `app/adapters/evidence.py:4` | Comment: nguồn `officer-confirmed` "chưa có nguồn ở ticket này" (WS1-3) | Cột được map thủ công bởi cán bộ có nhãn nguồn bằng chứng nhưng chưa có FK/trace cụ thể về ticket nào tạo ra map đó — vi phạm nhẹ nguyên tắc truy nguồn của dự án (CLAUDE.md: "mọi phát hiện phải có FK về dòng dữ liệu Tầng 1") | Bổ sung liên kết tới ticket/PR đã tạo cơ chế `saved_map` khi có thời gian; không chặn vận hành hiện tại |
| Thấp | `app/adapters/_common.py:237,243` | `except ValueError: pass` khi parse ngày kỳ báo cáo từ text tiêu đề file — im lặng bỏ qua nếu định dạng ngày trong file thô sai | `header.period_from`/`period_to` giữ `None`, nhưng đây là trường phụ trợ (không phải nguồn kỳ chính) nên tác động thấp | Không cần sửa gấp; nếu muốn chặt hơn có thể log ở mức debug khi bỏ qua |

## Ghi chú phạm vi

- Không rà `tests/test_dynamic_checks/`, `app/pipeline/`, `app/adapters/` sâu cho mục "dead code" — chỉ dừng ở 1 ví dụ tìm được (`not_evaluable`). Cần một lượt quét riêng, có ngân sách thời gian lớn hơn, nếu muốn liệt kê đầy đủ hàm/hằng số không còn ai gọi.
- Danh sách 22 route thiếu test được xác minh 2 lần (regex full-path + grep từ khoá rời) để loại trừ trường hợp test gọi route qua f-string chứa dấu nháy đơn làm regex đầu bỏ sót; vẫn có khả năng sai sót nhỏ nếu test gọi route qua helper gián tiếp không chứa chuỗi path trực tiếp.
